spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dmlssdb?serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    database: mysql
    properties:
      hibernate: 
        format_sql: true
    hibernate:
      ddl-auto: none